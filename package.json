{
  "name": "agent-commerce-midnight",
  "version": "0.6.0",
  "description": "Autonomous Agent Commerce & Escrow Protocol on Midnight Network",
  "private": true,
  "type": "module",
  "workspaces": [
    "contract",
    "ui"
  ],
  "scripts": {
    "compile:contract": "npm run compile --prefix contract",
    "test:contract": "npm run test --prefix contract",
    "test:ui": "npm run test --prefix ui",
    "test": "npm run test:contract",
    "build:contract": "npm run build --prefix contract",
    "dev:ui": "npm run dev --prefix ui",
    "build:ui": "npm run build --prefix ui",
    "typecheck": "npm run typecheck --prefix contract && npm run typecheck --prefix ui",
    "check:security": "node ./scripts/check-security.js",
    "deploy:preprod": "npx tsx scripts/deploy.ts",
    "ci": "npm run typecheck && npm run compile:contract && npm run test && npm run build:ui && npm run check:security",
    "build": "npm run build:contract && npm run build:ui"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.8.2",
    "vitest": "^3.0.7"
  }
}
